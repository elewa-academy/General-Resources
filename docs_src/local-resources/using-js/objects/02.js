//  with an object instead of a variable

var accObj = {
	cash: 0
};

// OBJECT LITERAL

// ---------------------------------- ///

console.log(accObj.cash);

accObj.cash++;

console.log(accObj.cash);

accObj.cash--;

console.log(accObj.cash);