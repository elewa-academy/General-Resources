Determine the logical relationship between each possible input type:

	1) Mutually Exclusive:
		- a property cannot be absent AND present.
		- a property cannot be the right type AND the wrong type.
	2) Logical Dependancy:
		- if a property is absent, it cannot be of the wrong or right type.
		- if a property is of the right OR wrong type, it cannot be absent.
	3) Locigally Independant:
		- the object having extra properties tells us nothing about the properties it should have